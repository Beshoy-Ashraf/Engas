# TODO
- [x] Verify whether `Microsoft.Extensions.Configuration.Json` package is needed and add it to `API/API.csproj` if missing (not found; will add next)
- [x] Update `API/appsettings.json` connection string to use provided Supabase host/port/db/user (and set password placeholder safely)


- [ ] Ensure ASP.NET loads configuration from `appsettings.json` (already handled by Web SDK; will keep verifying via build)

- [x] (Optional) Run `npx skills add supabase/agent-skills` if desired

- [x] Run `dotnet restore` and `dotnet build` to confirm everything compiles


